those run in async function
1. asyncio.create_task(f(x)) for async function, run immediately, get result with await

2. g() if g is async function, this will not run, untill await g()

3. loop=asyncio.get_event_loop() or get_running_loop() or new_event_loop(),
  loop.run_in_executor(None, lambda: f(x)) for blocking function in other thread, run immediately, get result with await

4. asyncio.to_thread(lambda: f(x)) sugar syntax for loop.run_in_executor

5. task = asyncio.ensure_future(f(x)), run blocking function in the current thread,
   tasks = set()
   tasks.add(task)
   task.add_done_callback(tasks.discard) # drop cache reference

those run in blocking function
asyncio.run(g())

--------------------
await asyncio.gather(*[ asyncio.create_task(f()) ])
that async def f() need return concret value not Task/Future,

# use async def to construct Task with concret value, like return 3:: Maybe Int
async def g()
    return 3

Right:
async def f():
    return await g()

Wrong:
async def f1():
    return g()

call async function g with g() would do nothing, until await g() it will run
but asyncio.create_task and loop.run_in_executor would run immediately without await
then await for its result

but also
[await i for i in [asyncio.create_task(f1())]] is ok 

but [await i for i in [f1(), f1()]] would be sequencial, not concurrent
--------------------------


in python, do not use threading inside async def, use asyncio.create_task to run block function inside async def and it will not block rest code

1. do not use threading inside async def
2. use asyncio.create_task to run another async function without await inside async def, and it will not block rest code
3. use loop.run_in_executor to run blocking function inside async def

`await` will yield control to event loop, other function inside event loop will run, but the `await` will block the rest code
until it get result

1. loop.run_in_executor() WITHOUT await - It WILL run immediately! return Future object, not block rest code
2. await loop.run_in_executor() - Runs AND returns the actual result (not a Future), block rest code
3. asyncio.create_task() WITHOUT await - Starts immediately, returns a Task object, not block rest code
4. await asyncio.create_task() - Starts immediately AND awaits the result, block rest code
