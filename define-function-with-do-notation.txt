two different ways

1. fun test() : Either<Error, Unit> { 
   ...
   return Either.right()
   }

2. fun test(): Either<Error, Unit> = either { ... }


fun t2() : ArrayList<T> = arraylistOf (...)

like do notation in Haskell

f :: Int -> Maybe Int
f x = do
    return x

or 
f x = if x /= 0 then Just x else Nothing