App.chat is Text field, but it stores json string like "{'enabled': true}"
how to filter rows that chat["enabled"] == true?

from sqlalchemy import cast
from sqlalchemy.dialects.postgresql import JSONB
db.session.query(App).select_from(App).filter(App.chat != "")
.filter(cast(App.chat, JSONB)['enabled'] == "true").all()
