lark group robot https://www.feishu.cn/hc/zh-CN/articles/360024984973#source=section

create a group, add a bot in setting of group, get the webhook api address

import requests,json
class Lark:
    def __init__(self):
        self.url = "https://open.feishu.cn/open-apis/bot/v2/hook/xxx"
        self.headers = {'Content-Type': 'application/json'}

    def notify(self,msg: str):
        data = {"msg_type":"text","content":{"text":msg}}
        res=requests.post(url=self.url,headers=self.headers,data=json.dumps(data))
        print(res.content)

#lark = Lark()
#lark.notify("hello")

