download PKGBUILD of package from aur.archlinux.org
and run 'makepkg -s' to generate xxx.tar.xz then 'sudo pacman -U xxx.tar.xz' install it

use git clone 'https://aur.archlinux.org/redsocks2-git.git' to download, 
don't use PKGBUILD file context, otherwise you will lose some files like redsocks.install


