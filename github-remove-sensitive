
https://rtyley.github.io/bfg-repo-cleaner/
download bfg.jar from https://repo1.maven.org/maven2/com/madgag/bfg/1.14.0/bfg-1.14.0.jar

#java -jar bfg.jar --strip-blobs-bigger-than 100M some-big-repo.git
#cd some-big-repo.git
#git reflog expire --expire=now --all && git gc --prune=now --aggressive
#git push

https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository

1. remove it on local
cd repo
git rm sensitive-file
git commit -m 'remove sensitive file'
java -jar bfg.jar --delete-files sensitive-file
git reflog expire --expire=now --all && git gc --prune=now --aggressive
git push --force

2. remove it on github
file a ticket to remove commit history on github
https://support.github.com/request?tags=docs-generic

https://support.github.com/request
choose account or bill issue, then file a report 

asking them to remove cached views and references to the sensitive data in pull requests on GitHub. Please provide the name of the repository and/or a link to the commit you need removed.

https://support.github.com/contact?flow=file_attachment_removal&subject=Remove%20file&tags=rr-remove-data

