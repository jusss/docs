in upload-files.cabal it may have two executable sections

executable upload-files
  main-is:             web12.hs
  ghc-options: -threaded -rtsopts
  other-modules:       Data.String.Split
  -- other-modules:
  -- other-extensions:
  build-depends:       base >=4.13 && <4.14, scotty, wai, text, transformers, directory, http-types, wai-extra, unix, bytestring, utf8-string, scotty-login-session, scotty-cookie, blaze-builder, binary, warp, process, time, scotty-tls
  -- hs-source-dirs:
  default-language:    Haskell2010


executable upload-files-another
  main-is:             web12-another.hs
  ghc-options: -threaded -rtsopts
  other-modules:       Data.String.Split
  -- other-modules:
  -- other-extensions:
  build-depends:       base >=4.13 && <4.14, scotty, wai, text, transformers, directory, http-types, wai-extra, unix, bytestring, utf8-string, scotty-login-session, scotty-cookie, blaze-builder, binary, warp, process, time, scotty-tls
  -- hs-source-dirs:
  default-language:    Haskell2010

then you can use 'cabal v2-run upload-files-another' to run web12-another.hs
'cabal v2-run upload-files' to run web12.hs

